# may_useful

这个文件夹用于收纳当前主控台架构里不直接参与运行、但可能仍有参考价值的文件。

## debug_tools

- 独立调试工具或历史实验性小工具。

## reference_assets

- 说明文档、映射文件、参考图片等。

## runtime_artifacts

- 历史 `error.txt` 和旧的 crash logs。

当前主控台 GUI 的运行入口、配置文件、后台服务、缓存层和兼容 wrapper 都仍保留在 `neural_recorder_GUI/` 根目录或新的分层包中，没有被移动到这里。
